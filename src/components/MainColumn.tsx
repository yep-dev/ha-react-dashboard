import { Stack } from '@components'
import styled from '@emotion/styled'

export const MainColumn = styled(Stack)`
  min-width: 417px;
  max-width: 417px;
  min-height: 540px;

  & > div {
    flex: none;
  }
`
